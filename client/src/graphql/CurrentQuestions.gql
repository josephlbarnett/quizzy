query currentQuestions {
    activeQuestions {
        id,
        authorId,
        body,
        activeAt,
        closedAt,
        response {
            id,
            response,
            ruleReferences
        }
    }
}